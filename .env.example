# ÖNEMLİ: Bu satırları düzenle!

NODE_ENV=development
PORT=5000

# MongoDB (Şimdilik bu)
MONGODB_URI=mongodb://localhost:27017/travelsync

# JWT Secret - DEĞİŞTİR!
JWT_ACCESS_SECRET=degistir-bunu-uzun-random-bir-string-yap
JWT_REFRESH_SECRET=bu-da-baska-bir-uzun-random-string

# JWT Expiry Times (optional, defaults: 15m and 7d)
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Frontend
CORS_ORIGIN=http://localhost:5173

# WhatsApp / Twilio (Flash Offer için)
# https://console.twilio.com adresinden alınır
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Email / SMTP Ayarları
# Gmail için: smtp.gmail.com:587 ve App Password gerekli
# SendGrid için: smtp.sendgrid.net:587
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-password-or-app-password
EMAIL_FROM=noreply@travelsync.com

# Stripe Ödeme (Escrow / Hold Payment)
# https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Klarna BNPL (Buy Now Pay Later)
# https://portal.klarna.com
KLARNA_API_KEY=
KLARNA_API_SECRET=
KLARNA_API_URL=https://api.playground.klarna.com

# Redis (Rate Limiting için - opsiyonel)
# Yoksa in-memory fallback kullanılır
REDIS_URL=redis://localhost:6379

# API Key (Harici API erişimi için - opsiyonel)
# Tanımlanmazsa API key kontrolü devre dışı kalır
API_KEY=
