# ⚠️ TypeScript Config - Kullanılmıyor

Bu proje **CommonJS (JavaScript)** kullanıyor, TypeScript kullanılmıyor.

## Neden Kaldırılmadı?

Gelecekte TypeScript'e geçiş yapılabilir diye saklanıyor.

## TypeScript'e Geçiş Yapmak İstersen:

1. `tsconfig.json`'ı aktif et
2. `package.json`'a TypeScript dependencies ekle
3. Tüm `.js` dosyalarını `.ts`'e çevir
4. Type definitions ekle

## Şu Anki Durum:

- ✅ CommonJS (`.js` dosyaları)
- ✅ `require()` / `module.exports`
- ❌ TypeScript kullanılmıyor
- ❌ `tsconfig.json` aktif değil

---

**Son Güncelleme:** 26 Ekim 2025

